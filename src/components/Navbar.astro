---
// src/components/Navbar.astro
interface NavItem {
  label: string;
  href: string;
}

// If it's the GitHub link, open it in a new tab; otherwise, keep it local
const navItems: NavItem[] = [
  { label: "About", href: "#about" },
  { label: "Experience", href: "#experience" },
  { label: "Projects", href: "#projects" },
  { label: "Contact", href: "#contact" },
  { label: "Github", href: "https://github.com/D3rrickla" },
];
---

<nav
  class="sticky top-0 z-50 h-24 w-full flex items-center px-6 backdrop-blur-xl bg-gradient-to-r from-slate-600/15 via-zunc-500/10 to-gray-900/10 before:absolute before:inset-0 before:bg-slate-950/40 before:-z-10 [mask-image:linear-gradient(to_bottom,rgba(0,0,0,1)_70%,rgba(0,0,0,0))] [-webkit-mask-image:linear-gradient(to_bottom,rgba(0,0,0,1)_70%,rgba(0,0,0,0))]"
>
  <div
    class="max-w-screen-2xl mx-auto w-full flex justify-between items-center relative"
  >
    <a
      href="#home"
      class="text-4xl font-bold text-brand-text tracking-tight hover:text-brand-primary transition-colors"
    >
      Derrick<span class="text-brand-primary"> La</span>
    </a>

    <input type="checkbox" id="menu-toggle" class="peer hidden" />

    <label
      for="menu-toggle"
      class="md:hidden cursor-pointer p-2 z-50 text-brand-text hover:text-brand-primary transition-colors"
      aria-label="Toggle Menu"
    >
      <svg
        class="w-6 h-6"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M4 6h16M4 12h16M4 18h16"></path>
      </svg>
    </label>

    <!-- Nav Items Links -->
    <!-- Responds to the checkbox state using Tailwind's peer-checked variants -->
    <div
      class="absolute top-14 left-0 right-0 hidden peer-checked:flex flex-col bg-brand-surface/95 border border-brand-muted/20 rounded-2xl p-6 gap-4 shadow-xl
                    md:relative md:top-0 md:flex md:flex-row md:bg-transparent md:border-none md:p-0 md:gap-6 md:shadow-none md:w-auto"
    >
      {
        navItems.map((item) => (
          <a
            href={item.href}
            target={item.href.startsWith("http") ? "_blank" : undefined}
            rel={
              item.href.startsWith("http") ? "noopener noreferrer" : undefined
            }
            class="nav-link text-brand-muted font-medium hover:text-brand-text transition-all relative py-1 text-base md:text-mm"
          >
            {item.label}
          </a>
        ))
      }
    </div>
  </div>
</nav>

<style>
  .nav-link::after {
    content: "";
    position: absolute;
    width: 0;
    height: 2px;
    bottom: 0;
    left: 0;
    background-color: var(--color-brand-primary);
    transition: width 0.3s ease;
  }

  .nav-link:hover::after {
    width: 100%;
  }
</style>
