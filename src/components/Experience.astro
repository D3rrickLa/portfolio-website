---
// basically we can have the definition defined here and then we import the data
// to be displayed

// aka, job experience
interface ExperienceProps {
    title: string;
    company: string;
    startDate: string; // Jan 2026
    endDate: string;  // Apr 2026
    description: string;
}
---